# Basic Scan Example

This example demonstrates a minimal REDINK execution using default settings.

## Purpose

Run a standard scan against a single target using:

- Default port configuration
- Default timeout (5 seconds)
- Default concurrency
- Normal output format

## Usage

```bash
redink example.com
```
